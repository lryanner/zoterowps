<customUI xmlns="http://schemas.microsoft.com/office/2006/01/customui" onLoad="OnAddinLoad">
    <ribbon startFromScratch="false">
        <tabs>
            <tab id="wpsAddinTab" label="zoterowps">
				<group id="btnDemoGroup" label="group1">
					<button id="InsertZoteroCitationButton" label="Add/Edit Citation" size="large" getImage="GetImage" onAction="OnAction" supertip="Insert a new citation, or edit the citation at the current cursor position"/>
					<button id="InsertZoteroNoteButton" label="Add Note" size="large" getImage="GetImage" onAction="OnAction" supertip="Insert a new note at the current cursor position"/>
					<button id="InsertZoteroBibliographyButton" label="Add/Edit Bibliography" size="large" getImage="GetImage" onAction="OnAction" supertip="Insert a new bibliography, or edit the existing bibliography, at the current cursor position"/>
					<button id="ZoteroSetDocPrefs" label="Document Preferences" getImage="GetImage" onAction="OnAction" supertip="Change the citation style or locale"/>
					<button id="RefreshZotero" label="Refresh" getImage="GetImage" onAction="OnAction" supertip="Update all citations to reflect changes"/>
					<button id="ZoteroRemoveCodes" label="Unlink Citations" getImage="GetImage" onAction="OnActions" supertip="Remove all Zotero field codes and unlink from Zotero library"/>
				</group>
            </tab>
        </tabs>
    </ribbon>
</customUI>